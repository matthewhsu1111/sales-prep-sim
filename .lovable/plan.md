
The user wants to see a bigger version of the favicon I just generated. I'll generate a larger PNG (e.g. 512x512) of the same navy + off-white mic favicon and save it to `/mnt/documents/` so they can preview/download it as an artifact.

**Plan:**
1. Generate a 512x512 PNG with Navy Midnight (#1A1F3C) rounded-square background and an off-white (#F5F4F0) microphone icon centered, matching the favicon design.
2. Save to `/mnt/documents/cadence-favicon-large.png`.
3. Surface it as a `presentation-artifact` so you can preview/download it.

No code changes to the project — this is purely a preview artifact.
